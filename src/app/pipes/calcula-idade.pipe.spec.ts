import { CalculaIdadePipe } from './calcula-idade.pipe';

describe('CalculaIdadePipe', () => {
  it('create an instance', () => {
    const pipe = new CalculaIdadePipe();
    expect(pipe).toBeTruthy();
  });
});
